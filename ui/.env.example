# ── OIDC Authentication (Logto / Zitadel / any OIDC provider) ──
# Set these to enable SSO login. Leave VITE_OIDC_CLIENT_ID empty for token-only auth.
VITE_OIDC_ISSUER=http://localhost:3001
VITE_OIDC_CLIENT_ID=
